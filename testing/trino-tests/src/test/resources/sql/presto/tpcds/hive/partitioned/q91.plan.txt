remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            final aggregation over (cc_call_center_id, cc_manager, cc_name, cd_education_status, cd_marital_status)
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, ["cc_call_center_id", "cc_manager", "cc_name", "cd_education_status", "cd_marital_status"])
                        partial aggregation over (cc_call_center_id, cc_manager, cc_name, cd_education_status, cd_marital_status)
                            join ([c_current_hdemo_sk = hd_demo_sk], INNER, REPLICATED, [DF_1957]):
                                join ([cr_call_center_sk = cc_call_center_sk], INNER, REPLICATED, [DF_1958]):
                                    join ([c_customer_sk = cr_returning_customer_sk], INNER, PARTITIONED, [DF_1959]):
                                        remote exchange (REPARTITION, HASH, ["c_customer_sk"])
                                            join ([c_current_addr_sk = ca_address_sk], INNER, PARTITIONED, [DF_1960]):
                                                remote exchange (REPARTITION, HASH, ["c_current_addr_sk"])
                                                    join ([c_current_cdemo_sk = cd_demo_sk], INNER, REPLICATED, [DF_1961]):
                                                        filter [df_1957, df_1959, df_1960, df_1961]
                                                            scan customer
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPLICATE, BROADCAST, [])
                                                                filter []
                                                                    scan customer_demographics
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, ["ca_address_sk"])
                                                        filter []
                                                            scan customer_address
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPARTITION, HASH, ["cr_returning_customer_sk"])
                                                join ([cr_returned_date_sk = d_date_sk], INNER, REPLICATED, [DF_1971]):
                                                    filter [df_1958, df_1971]
                                                        scan catalog_returns
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            filter []
                                                                scan date_dim
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            scan call_center
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPLICATE, BROADCAST, [])
                                        filter []
                                            scan household_demographics
