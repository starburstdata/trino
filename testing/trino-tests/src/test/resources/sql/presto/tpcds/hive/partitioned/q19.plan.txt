local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        final aggregation over (i_brand, i_brand_id, i_manufact, i_manufact_id)
            local exchange (GATHER, SINGLE, [])
                remote exchange (REPARTITION, HASH, ["i_brand", "i_brand_id", "i_manufact", "i_manufact_id"])
                    partial aggregation over (i_brand, i_brand_id, i_manufact, i_manufact_id)
                        join ([ca_address_sk = c_current_addr_sk], INNER, REPLICATED, [DF_1581]):
                            filter [df_1581]
                                scan customer_address
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    join ([c_customer_sk = ss_customer_sk], INNER, REPLICATED, [DF_1583]):
                                        filter [df_1583]
                                            scan customer
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                join ([ss_store_sk = s_store_sk], INNER, REPLICATED, [DF_1585]):
                                                    join ([ss_item_sk = i_item_sk], INNER, REPLICATED, [DF_1586]):
                                                        join ([ss_sold_date_sk = d_date_sk], INNER, REPLICATED, [DF_1587]):
                                                            filter [df_1585, df_1586, df_1587]
                                                                scan store_sales
                                                            local exchange (GATHER, SINGLE, [])
                                                                remote exchange (REPLICATE, BROADCAST, [])
                                                                    filter []
                                                                        scan date_dim
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPLICATE, BROADCAST, [])
                                                                filter []
                                                                    scan item
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            scan store
