local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        final aggregation over (d_year, i_brand, i_brand_id)
            local exchange (GATHER, SINGLE, [])
                remote exchange (REPARTITION, HASH, ["d_year", "i_brand", "i_brand_id"])
                    partial aggregation over (d_year, i_brand, i_brand_id)
                        join ([ss_item_sk = i_item_sk], INNER, REPLICATED, [DF_881]):
                            join ([ss_sold_date_sk = d_date_sk], INNER, REPLICATED, [DF_882]):
                                filter [df_881, df_882]
                                    scan store_sales
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPLICATE, BROADCAST, [])
                                        filter []
                                            scan date_dim
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    filter []
                                        scan item
