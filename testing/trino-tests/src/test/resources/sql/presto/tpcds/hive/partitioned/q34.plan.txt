remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            join ([c_customer_sk = ss_customer_sk], INNER, REPLICATED, [DF_1498]):
                filter [df_1498]
                    scan customer
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPLICATE, BROADCAST, [])
                        filter []
                            final aggregation over (ss_customer_sk, ss_ticket_number)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, ["ss_customer_sk", "ss_ticket_number"])
                                        partial aggregation over (ss_customer_sk, ss_ticket_number)
                                            join ([ss_hdemo_sk = hd_demo_sk], INNER, REPLICATED, [DF_1500]):
                                                join ([ss_store_sk = s_store_sk], INNER, REPLICATED, [DF_1501]):
                                                    join ([ss_sold_date_sk = d_date_sk], INNER, REPLICATED, [DF_1502]):
                                                        filter [df_1500, df_1501, df_1502]
                                                            scan store_sales
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPLICATE, BROADCAST, [])
                                                                filter []
                                                                    scan date_dim
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            filter []
                                                                scan store
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        filter []
                                                            scan household_demographics
