local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        final aggregation over (s_city, s_company_id, s_county, s_state, s_store_name, s_street_name, s_street_number, s_street_type, s_suite_number, s_zip)
            local exchange (GATHER, SINGLE, [])
                remote exchange (REPARTITION, HASH, ["s_city", "s_company_id", "s_county", "s_state", "s_store_name", "s_street_name", "s_street_number", "s_street_type", "s_suite_number", "s_zip"])
                    partial aggregation over (s_city, s_company_id, s_county, s_state, s_store_name, s_street_name, s_street_number, s_street_type, s_suite_number, s_zip)
                        join ([ss_store_sk = s_store_sk], INNER, REPLICATED, [DF_1272]):
                            join ([ss_sold_date_sk = d_date_sk], INNER, REPLICATED, [DF_1273]):
                                join ([ss_ticket_number = sr_ticket_number, ss_item_sk = sr_item_sk, ss_customer_sk = sr_customer_sk], INNER, REPLICATED, [DF_1274, DF_1275, DF_1276]):
                                    filter [df_1272, df_1273, df_1274, df_1275, df_1276]
                                        scan store_sales
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            join ([sr_returned_date_sk = d_date_sk_10], INNER, REPLICATED, [DF_1278]):
                                                filter [df_1278]
                                                    scan store_returns
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        filter []
                                                            scan date_dim
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPLICATE, BROADCAST, [])
                                        filter []
                                            scan date_dim
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    scan store
