local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        final aggregation over (i_item_id)
            local exchange (GATHER, SINGLE, [])
                remote exchange (REPARTITION, HASH, ["i_item_id"])
                    partial aggregation over (i_item_id)
                        join ([cs_item_sk = i_item_sk], INNER, PARTITIONED, [DF_1455]):
                            remote exchange (REPARTITION, HASH, ["cs_item_sk"])
                                join ([cs_promo_sk = p_promo_sk], INNER, REPLICATED, [DF_1456]):
                                    join ([cs_bill_cdemo_sk = cd_demo_sk], INNER, REPLICATED, [DF_1457]):
                                        join ([cs_sold_date_sk = d_date_sk], INNER, REPLICATED, [DF_1458]):
                                            filter [df_1455, df_1456, df_1457, df_1458]
                                                scan catalog_sales
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    filter []
                                                        scan date_dim
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                filter []
                                                    scan customer_demographics
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            filter []
                                                scan promotion
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, ["i_item_sk"])
                                    scan item
